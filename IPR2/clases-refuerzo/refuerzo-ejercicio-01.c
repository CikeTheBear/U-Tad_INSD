#include <stdio.h>

#define NUM_MAX_CARACTERES 16

int validaLogin(char login[NUM_MAX_CARACTERES], int* numMay, int* numMin, int* numDig, int* numGuiones);

void generaPassword(char login[NUM_MAX_CARACTERES], char password[NUM_MAX_CARACTERES]);

int longitudCadena(char cadena[NUM_MAX_CARACTERES]);



int main() {


    return 0;
}